if (condición) {
    // lo que se ejecutará si la condición es true
} else if (2° condición) { // podemos tener 0 o muchas de estas condiciones
    // lo que se ejecutará si la segunda condición es true
} else { // podemos tener 0 o 1 de esta instrucción
    // lo que se ejecutará si ninguna de las condiciones anteriores es verdadera.
}

for (var a = 0; a < 8; a++) { // a=0
    console.log(a); // 0, 1, 2, 3, 4, 5, 6, 7, 8
}